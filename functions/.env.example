# Local emulators use the checked-in fixture by default.
# A live local request additionally requires data.source == "live" and a real
# value in .secret.local. Keep this false for normal development and CI.
ALLOW_LIVE_FOOTBALL_API=false
